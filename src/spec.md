# Specification

## Summary
**Goal:** Add Bhatoa Traders branding images (logo and hero banner) to the existing portfolio frontend.

**Planned changes:**
- Add the provided/generated branding image files under `frontend/public/assets/generated` with the exact required filenames and dimensions.
- Ensure the existing UI renders the logo in the header/footer and the hero banner in the hero section without layout issues on mobile or desktop.

**User-visible outcome:** The site displays the Bhatoa Traders logo and hero banner in the existing header/footer and hero section, with the assets served at the required `/assets/generated/...` paths without 404s.
